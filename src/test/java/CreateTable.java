/* public class CreateTable {
create table domashkaMembers
(id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
            name VARCHAR(15),
            age int(3),
            legsize int(2),
            sex VARCHAR(3),
            status VARCHAR(15),
            pension int(2));

 */
